# 5 4 3

#--------------------------------------------------------
# Input: a list of numbers
# Output: the number of items in the list
#--------------------------------------------------------
def count_items(L):
	# initialize count
	count = 0

	# iterate through each item of L and update count
	for x in L:
		# increment count

	# return the cummulative count
	return count


