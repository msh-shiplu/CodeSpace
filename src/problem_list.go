package main

import "net/http"

func problemListHandler(w http.ResponseWriter, r *http.Request, who string, uid int) {
	// role := r.FormValue("role")
	// rows, err:= Database.Query("select * from problem")
	// if err != nil {
	// 	log.Fatal(err)
	// }
	// for rows.Next() {
	// 	rows.Scan(&vote)
	// }
}
